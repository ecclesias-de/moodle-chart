apiVersion: v1
kind: Service
metadata:
  name: {{ include "moodle.basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "moodle.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  # clusterIP: None
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector: {{- include "moodle.matchLabels" . | nindent 4 }}

